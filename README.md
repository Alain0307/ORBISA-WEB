# ORBISA WEB

Para correr la página localmente
1.Entra a la carpeta 
cd orbisa-tech

2. Instala dependencias (solo la primera vez)
npm install

3. Arranca el servidor
npm run dev

4. Abrir en el navegador
# http://localhost:5174
